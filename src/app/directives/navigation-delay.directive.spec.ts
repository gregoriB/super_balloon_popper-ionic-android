import { NavigationDelayDirective } from './navigation-delay.directive';

describe('NavigationDelayDirective', () => {
    it('should create an instance', () => {
        const directive = new NavigationDelayDirective();
        expect(directive).toBeTruthy();
    });
});
